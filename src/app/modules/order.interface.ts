// 1. Create an interface representing a document in MongoDB.
export type TOrder = {
  quantityInStock: number;
  email: string;
  productId: string;
  price: number;
  quantity: number;
  iStock: number;
};
